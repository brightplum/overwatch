/**
 * @file
 * Overwatch BP behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.overwatchBp = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
